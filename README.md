# EnergyCommunities project
### Usage
To start the entire application (db + backend + frontend): `docker-compose up --build` in your terminal.<br> 
To shutdown the system `docker-compose down`.<br>
To access to the endpoint debug: `http://localhost:8080/swagger-ui/index.html`.<br>
Angular on: `localhost:4200`.